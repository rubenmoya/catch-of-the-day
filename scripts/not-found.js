var React = require('react');
var PropTypes = React.PropTypes;

var NotFound = React.createClass({

  render: function() {
    return (
      <h1>404 Page (component) not found</h1>
    );
  }

});

module.exports = NotFound;
